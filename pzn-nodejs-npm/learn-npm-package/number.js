export const min = (first, second) => {
  if (first < second) {
    return first
  }
  return second
}

export const max = (first, second) => {
  if (first > second) {
    return first
  }
  return second
}
